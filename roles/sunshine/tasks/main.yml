---
# Role: sunshine
# Sunshine game stream host (AUR) — VA-API/KWin capture on AMD Wayland desktop.
# Installs package, scripts, config, and enables user service.

# ── Dependencies ───────────────────────────────────────────────────────────────

- name: Add user to input and realtime groups (Sunshine needs /dev/input access and priority scheduling)
  ansible.builtin.user:
    name: "{{ cachyos_user }}"
    groups:
      - input
      - realtime
    append: true
  tags: [sunshine, groups]

- name: Allow Sunshine encoding threads to reach nice -15 (override realtime group's -11 cap)
  ansible.builtin.copy:
    content: |
      # Allow {{ cachyos_user }} to set process priority as low as nice -20.
      # Sunshine's encoding threads request nice -15; the realtime group only allows -11.
      {{ cachyos_user }} - nice -20
    dest: /etc/security/limits.d/91-sunshine-nice.conf
    owner: root
    group: root
    mode: "0644"
  tags: [sunshine, groups]

- name: Install sunshine from AUR
  ansible.builtin.shell: |
    set -euo pipefail
    paru -S --noconfirm --needed sunshine
  args:
    executable: /bin/bash
  become: false
  register: sunshine_aur_result
  changed_when: "'there is nothing to do' not in sunshine_aur_result.stdout"
  tags: [sunshine, aur]

# ── Scripts ────────────────────────────────────────────────────────────────────

- name: Install sunshine prep/undo scripts from joystick-notify repo
  ansible.builtin.copy:
    src: "{{ cachyos_joystick_notify_src }}/sunshine/{{ item }}"
    dest: "/usr/local/bin/{{ item }}"
    owner: root
    group: root
    mode: "0755"
  loop:
    - sunshine-couch-prep.sh
    - sunshine-couch-undo.sh
    # Shared with joystick-notify's local couch-mode activate/teardown (lib/status-notify.sh
    # set_screen_lock) — prevents the screen locking mid-session, on either trigger path.
    - couch-mode-screen-unlock.sh
    # Holds ScreenSaver.Inhibit() cookie for the session lifetime; started by
    # sunshine-couch-prep.sh and killed by sunshine-couch-undo.sh.
    - screen-lock-inhibit-daemon.sh
    # Integration test — run manually to verify the unlock script works end-to-end.
    - test-screen-unlock.sh
  tags: [sunshine, scripts]

- name: Install launch-bigpicture script (used by sunshine apps.json)
  ansible.builtin.copy:
    src: "{{ cachyos_joystick_notify_src }}/scripts/launch-bigpicture.sh"
    dest: /usr/local/bin/launch-bigpicture.sh
    owner: root
    group: root
    mode: "0755"
  tags: [sunshine, scripts]

# Virtual-display scripts — see roles/sunshine/files/sunshine-display-backend.sh
# for why: Sunshine's dd_resolution_option automatic resolution switching does
# not work under capture=kwin (confirmed via zero display-device log activity
# during a live session — the physical output never actually resized). A
# krfb-virtualmonitor output created at the client's requested resolution and
# captured directly sidesteps the issue entirely, and applies uniformly to
# every app/client (Steam Deck, 4K TV, bedroom TV) since Sunshine's capture
# target is a single global setting, not per-app.
- name: Install sunshine virtual-display scripts
  ansible.builtin.copy:
    src: "{{ item }}"
    dest: "/usr/local/bin/{{ item }}"
    owner: root
    group: root
    mode: "0755"
  loop:
    - sunshine-display-backend.sh
    - sunshine-vdisplay-connect.sh
    - sunshine-vdisplay-disconnect.sh
  tags: [sunshine, scripts]

# cursor-stream-hide/show scripts — install only if present in the repo
- name: Install cursor-stream scripts if present in sunshine dir
  ansible.builtin.copy:
    src: "{{ item }}"
    dest: "/usr/local/bin/{{ item | basename }}"
    owner: root
    group: root
    mode: "0755"
  with_fileglob:
    - "{{ cachyos_joystick_notify_src }}/sunshine/cursor-stream-*.sh"
  tags: [sunshine, scripts]

# ── Config ─────────────────────────────────────────────────────────────────────

- name: Ensure ~/.config/sunshine exists
  ansible.builtin.file:
    path: "{{ cachyos_user_home }}/.config/sunshine"
    state: directory
    owner: "{{ cachyos_user }}"
    group: "{{ cachyos_user }}"
    mode: "0755"
  become: false
  tags: [sunshine, config]

- name: Install sunshine.conf
  ansible.builtin.template:
    src: sunshine.conf.j2
    dest: "{{ cachyos_user_home }}/.config/sunshine/sunshine.conf"
    owner: "{{ cachyos_user }}"
    group: "{{ cachyos_user }}"
    mode: "0644"
  become: false
  notify: Restart sunshine service
  tags: [sunshine, config]

- name: Install sunshine apps.json
  ansible.builtin.copy:
    src: apps.json
    dest: "{{ cachyos_user_home }}/.config/sunshine/apps.json"
    owner: "{{ cachyos_user }}"
    group: "{{ cachyos_user }}"
    mode: "0644"
  become: false
  notify: Restart sunshine service
  tags: [sunshine, config]

# ── Firewall (UFW) ────────────────────────────────────────────────────────────

- name: Allow Sunshine TCP ports through UFW
  community.general.ufw:
    rule: allow
    port: "{{ item }}"
    proto: tcp
    comment: "Sunshine streaming"
  loop:
    - "47984"   # HTTPS
    - "47989"   # HTTP
    - "47990"   # web UI
    - "48010"   # RTSP
  tags: [sunshine, ufw]

- name: Allow Sunshine UDP ports through UFW
  community.general.ufw:
    rule: allow
    port: "{{ item }}"
    proto: udp
    comment: "Sunshine streaming"
  loop:
    - "47998"   # video
    - "47999"   # control
    - "48000"   # audio
    - "48002"   # input
    - "48010"   # mic
  tags: [sunshine, ufw]

# ── mDNS (Moonlight host discovery) ─────────────────────────────────────────────
# Moonlight's "auto-discover hosts on your network" relies on mDNS: Sunshine
# publishes _nvstream._tcp via avahi, and the Moonlight client both sends its
# query and receives the reply over UDP 5353. ufw's default-deny INPUT policy
# silently drops that traffic on either end -- diagnosed 2026-09-06 when
# Moonlight on a client (lemurpro) never found this host even though avahi on
# the Sunshine host was publishing the record correctly the whole time
# (avahi-browse run locally on the host sees its own advertisement regardless
# of ufw, which is what made this look like a working avahi with no obvious
# cause -- it's specifically the *inbound* UDP 5353 packet from the other
# host that ufw was dropping). Tagged separately (not just `sunshine`) since
# this same rule is also applied to Moonlight-client-only hosts in this play
# that don't get the rest of this role -- see setup-cachyos.yml's mdns tag.
- name: Allow mDNS (avahi, UDP 5353) through UFW for Sunshine/Moonlight discovery
  community.general.ufw:
    rule: allow
    port: "5353"
    proto: udp
    comment: "mDNS discovery (Sunshine/Moonlight)"
  tags: [sunshine, mdns]

# The UFW rule above fixed the transport, but Moonlight-qt still failed to
# connect after discovering the host -- diagnosed 2026-09-06 via
# moonlight-qt's own stdout log: it discovered "archlinux-4.local." over
# mDNS and resolved it, but the ONLY address avahi handed back in that
# resolution was this host's auto-assigned IPv6 ULA (fd00::/8, e.g.
# fd84:dede:...), not its IPv4 address -- even though the raw mDNS
# response packet (confirmed via tcpdump) carries both the A and AAAA
# records together. moonlight-qt's connection code explicitly skips ULA
# addresses when picking a "global IPv6" address (see
# ComputerManager::getBestGlobalAddressV6 in its source), but still falls
# back to using that same ULA as the connection address when no IPv4
# candidate came back from its resolver -- and that ULA is a per-interface
# autoconfigured address with no route from any other host on the LAN
# (confirmed: connecting to it from lemurpro fails immediately with
# "Network is unreachable", not a timeout). Disabling IPv6 in avahi
# forces it to publish/answer IPv4-only, sidestepping whatever specific
# quirk in Moonlight's mDNS resolver was preferring or exclusively
# returning the unusable ULA. Scoped to archlinux only -- this is about
# what the Sunshine *host* publishes, not anything on Moonlight-client
# hosts like lemurpro.
- name: Disable IPv6 in avahi so Sunshine's mDNS record only advertises the reachable IPv4 address
  ansible.builtin.lineinfile:
    path: /etc/avahi/avahi-daemon.conf
    regexp: '^use-ipv6='
    line: 'use-ipv6=no'
  when: inventory_hostname == 'archlinux'
  notify:
    - Restart avahi-daemon
    - Restart sunshine service
  tags: [sunshine, mdns]

# ── Udev / cap_sys_admin ───────────────────────────────────────────────────────

- name: Add udev rule so Sunshine can access /dev/uinput without root
  ansible.builtin.copy:
    content: |
      KERNEL=="uinput", GROUP="input", MODE="0660", OPTIONS+="static_node=uinput"
    dest: /etc/udev/rules.d/85-sunshine-uinput.rules
    owner: root
    group: root
    mode: "0644"
  notify: Reload udev rules
  tags: [sunshine, udev]

# ── Service ────────────────────────────────────────────────────────────────────

- name: Enable user lingering so systemd user instance runs without active login
  ansible.builtin.command: loginctl enable-linger {{ cachyos_user }}
  changed_when: false
  tags: [sunshine, service]

- name: Wait for user systemd bus socket
  ansible.builtin.wait_for:
    path: "/run/user/{{ cachyos_user_uid }}/bus"
    timeout: 10
  become: false
  tags: [sunshine, service]

- name: Reload systemd user daemon
  ansible.builtin.systemd:
    daemon_reload: true
    scope: user
  become: false
  environment:
    XDG_RUNTIME_DIR: "/run/user/{{ cachyos_user_uid }}"
    DBUS_SESSION_BUS_ADDRESS: "unix:path=/run/user/{{ cachyos_user_uid }}/bus"
  tags: [sunshine, service]

- name: Enable and start sunshine user service
  ansible.builtin.systemd:
    # The AUR sunshine package ships app-dev.lizardbyte.app.Sunshine.service, not
    # sunshine.service — verified via `pacman -Ql sunshine | grep systemd`.
    name: app-dev.lizardbyte.app.Sunshine.service
    enabled: true
    state: started
    scope: user
  become: false
  environment:
    XDG_RUNTIME_DIR: "/run/user/{{ cachyos_user_uid }}"
    DBUS_SESSION_BUS_ADDRESS: "unix:path=/run/user/{{ cachyos_user_uid }}/bus"
  tags: [sunshine, service]

# ── Handlers ───────────────────────────────────────────────────────────────────
# (inline — handlers not supported in role tasks list directly; use notify target)
