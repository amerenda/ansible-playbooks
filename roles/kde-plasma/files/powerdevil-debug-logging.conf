[Service]
# dpms-sleep-fix.service depends on this debug line to detect the DPMS
# trigger event: "DPMS: triggered on idle timeout". Must stay enabled.
Environment=QT_LOGGING_RULES=org.kde.powerdevil*=true
